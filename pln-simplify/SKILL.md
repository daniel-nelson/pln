---
name: pln-simplify
description: Map and safely simplify a codebase through pln's verified lifecycle. Invoke as `/pln-simplify`.
---

# pln-simplify — not built yet

This tracked file is a placeholder. Run the `setup` script in the parent pln directory to generate the host-specific skill from `src/`; then restart the agent. Do not run a simplification from this placeholder.
