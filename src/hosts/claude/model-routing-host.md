**Claude resolution.** Use `{{SKILL_DIR}}/bin/pln-model-route resolve --host claude` with the semantic profile, the current session model, and current effort. Opus and Fable satisfy the judgment floor and stay inherited; a recognized Sonnet or Haiku hosting model resolves judgment to the current `fable` alias at `high` effort. Opted-in evidence resolves to `sonnet` at `low` effort. These aliases live in `src/hosts/claude/model-profiles`, not shared policy.

For native Agent or Workflow calls, omit the model option when `MODEL_ARGUMENT=inherit`; otherwise pass the returned model, and pass the returned effort. If judgment capability is unknown or the selected frontier model/effort is rejected, stop before dispatch and ask whether to inherit for this run; never count a silent inherited fallback as meeting the floor. An unavailable economy selection falls back to inherit without blocking. After completion, prefer the model/effort the host reports; otherwise record `selected:<alias>; underlying model unreported`.
