Dispatch from `fix-manifest.tsv`, not an improvised file grouping. For every multi-node isolated wave, start one fresh, stably named background `Agent` per cluster with `isolation: "worktree"`; record Agent ID and the native worktree path before launching the next. If the path is not durably exposed, serialize that cluster in the original tree. Original-tree clusters run alone. Every brief names its exact lease and has `commit owner: coordinator`; workers touch neither git nor `REVIEW.md`.
