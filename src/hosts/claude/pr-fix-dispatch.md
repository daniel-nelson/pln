Then run the fixes. **Cluster the to-fix findings by file/subsystem** — findings touching the same files go in one cluster, so no two fix agents edit the same file. Start one fresh, stably named background `Agent` per cluster in the same turn. This is genuine native parallelism, but it uses directly addressable Agents rather than Workflow because a cluster may need a user answer and Workflow has no mid-run input channel. Each cluster only edits and verifies its leased files; agents touch neither git nor the shared `REVIEW.md`. Each cluster's fix subagent brief:
