Then run the fixes. **Cluster the to-fix findings by file/subsystem** — findings touching the same files go in one cluster, so no two fix agents edit the same file. Build **one Workflow script** that runs the clusters through `parallel()` — each cluster only edits and verifies its own files, so disjoint clusters have nothing to race on. Git is the one shared resource in the tree, and the agents don't touch it (see below); the orchestrator commits each cluster once its agent returns, one commit at a time. Each cluster's fix subagent brief:
