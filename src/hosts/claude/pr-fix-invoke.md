Wait until every cluster in the wave completes, blocks, or fails. Validate result and lease before the coordinator stages explicit paths and commits each completed cluster in its own worktree. Record its result/route/commit as checkpointed, then integrate commits in manifest order and update `REVIEW.md` alone. A blocked cluster freezes new dispatch; successful isolated siblings may checkpoint but do not integrate. Persist its handoff/worktree/Agent ID, ask one question, then `SendMessage` the answer to the same Agent; if the ID is gone, start a fresh Agent on the retained artifacts. Unexpected writes, conflicts, malformed output, or a corrected premise force a fresh boundary. The older/disabled native fallback is serial.
