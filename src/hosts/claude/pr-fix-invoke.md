Wait for the cluster's completion notification. A failed notification, empty result, malformed output, or write outside the lease fails that node; preserve its artifacts and force a fresh boundary. Otherwise the coordinator validates the result and diff, stages only the leased paths, and commits the cluster with the required co-author trailer; record its result/route/commit as checkpointed, integrate it after dirty-state validation, and publish the complete next-generation `REVIEW.md` candidate through `pln-publish-review` before the next cluster starts. When the next cluster is a `reuse` node in the same cohort, `SendMessage` that Agent ID with its brief; otherwise start a fresh Agent. A blocked cluster stops new dispatch: persist its handoff and Agent ID through the same publisher, ask one question, then `SendMessage` the answer to the same Agent; if the ID is gone, start a fresh Agent on the retained artifacts. Unexpected writes, conflicts, malformed output, or a corrected premise force a fresh boundary. The older/disabled native fallback is serial.
