**Host note.** This copy of `/pln-pr` was generated for Claude Code. Independent review fan-outs use Workflow; resumable fix work and single verification runs use named Agent calls (see Spawning a fresh-context agent). If a current native tool is absent because the host is older or policy-disabled, disclose the guarded `pln-claude-agent` fallback. If the whole tool surface identifies a different host instead, point the user at `./setup` in the pln directory one level up rather than improvising a mixed-host run.
