### Step 0.5. Host check — the right skill for the right host

This copy of `/pln-pr` was generated for Claude Code, and it runs its review army, fix pass, and verification on the harness's `Workflow` and `Agent` tools (see Spawning a fresh-context agent). Before dispatching any review work, confirm those tools are in your toolset. If they are not, this file was generated for a host you are not on — print exactly this and stop, rather than stalling partway through a run:

> This copy of /pln-pr was built for Claude Code, but this host has no Workflow/Agent tools. Re-run `./setup` in the pln install for this host, or review the branch manually.

On Claude Code the tools are present, so this check is a no-op and you continue to Step 1.

