Dispatch exactly the validated same-model roster slots through one Workflow `pipeline(briefs, brief => agent(brief.prompt, { label: brief.label, schema: findingsSchema }))`. Each is a fresh judgment worker inheriting the hosting model, with a distinct raw evidence path; the returned array preserves `null` for a failed slot. Do not pad the roster with inapplicable lenses. The peer adversarial slot, when permitted, runs separately through `pln-peer` and substitutes for the local adversarial brief rather than adding a reader.
