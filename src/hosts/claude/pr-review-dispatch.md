Otherwise dispatch the full army as **one Workflow script** that runs all reviewers through `pipeline(briefs, brief => agent(brief.prompt, { label: brief.label, schema: findingsSchema }))`. That is current Workflow's parallel fan-out surface: each reviewer is a fresh subagent, and the returned array preserves a `null` for any stopped or unrecoverably failed reviewer. Six lenses plus one adversarial generalist:
