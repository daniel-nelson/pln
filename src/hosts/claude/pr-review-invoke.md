Invoke the roster script through Workflow; state the exact role count and, as the concrete signal that drove it rather than as a tier name, how deep the review goes, in one sentence — plus that the reviewers' live activity is in `/workflows` — then wait for its notification rather than polling. Give every reviewer a distinct raw evidence path and require only `RESULT_FILE=<that path>`. Workflow carries pointers and fixed completion metadata, never findings. The merge worker validates each artifact and controls the fail-closed gate.
