Invoke the script through the Workflow tool; state it in one sentence ("Reviewing the diff with 7 fresh reviewers") and wait for its notification rather than polling. Give every reviewer a distinct raw evidence path and require its result to be only `RESULT_FILE=<that path>`. The Workflow carries pointers and fixed completion metadata, never findings. If an individual reviewer fails, continue with the rest; the merge worker validates each artifact, counts valid empty arrays as successful reviews, and controls the fail-closed gate.
