**Resolve pln's helpers**: use `${CLAUDE_SKILL_DIR}` when it contains `bin/pln-update-check`; otherwise check `$HOME/.agents/skills/pln`, `$HOME/.claude/skills/pln`, `.agents/skills/pln`, and `.claude/skills/pln` in order. Call the selected root `_PLN_DIR`. If none contains the executable, `_PLN_DIR` is empty and the readiness sweep takes its explicit `HELPER_ABSENT` degraded path.
