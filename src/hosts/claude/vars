# Literal substitutions for the claude host. KEY=value, one per line.
# Values are inserted verbatim wherever {{KEY}} appears in a core file or a
# fragment. No expressions, no escapes — keep them plain.
SKILL_DIR=${CLAUDE_SKILL_DIR}
HOST_NAME=Claude Code
HOST_CLI=claude
NOTIFY_CALL=call `PushNotification` and run `pln-notify-desktop`
ORCH_TOOLS=the harness Agent/Workflow tools
PLN_CMD=/pln
PLN_PR_CMD=/pln-pr
PLN_SIMPLIFY_CMD=/pln-simplify
PLN_UPDATE_CMD=/pln-update
