**Codex resolution.** Use `{{SKILL_DIR}}/bin/pln-model-route resolve --host codex` with the semantic profile, the current session model when the host exposes it, and current reasoning effort. Judgment always inherits the hosting model and requests at least `high` effort; an unreported or unfamiliar model name is ordinary attribution, never a dispatch gate. Opted-in evidence resolves to `gpt-5.6-luna` at `low` effort when that exact route is release-qualified. Economy identifiers live in `src/hosts/codex/model-profiles`, not shared policy.

For `spawn_agent`, omit `model` when `MODEL_ARGUMENT=inherit`; otherwise pass the returned economy model, and pass `reasoning_effort` from the returned effort. The nested fallback follows the same rule through `pln-codex-agent`. Never ask permission to inherit a judgment worker and never infer capability from an alias. An unavailable economy selection falls back to inherit without blocking. After completion, prefer the model/effort the host reports; otherwise record the inherited or selected alias and `underlying model unreported`.
