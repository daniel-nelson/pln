**Codex resolution.** Use `{{SKILL_DIR}}/bin/pln-model-route resolve --host codex` with the semantic profile, the current session model, and current reasoning effort. A current `gpt-5.6-sol` selection satisfies the judgment floor and stays inherited; a recognized economy-tier hosting model resolves judgment to `gpt-5.6-sol` at `high` effort. Opted-in evidence resolves to `gpt-5.6-luna` at `low` effort. These identifiers live in `src/hosts/codex/model-profiles`, not shared policy.

For `spawn_agent`, omit `model` when `MODEL_ARGUMENT=inherit`; otherwise pass the returned model, and pass `reasoning_effort` from the returned effort. The nested fallback passes the same values through `pln-codex-agent --model ... --effort ... --profile ...`. If judgment capability is unknown or the selected frontier model/effort is rejected, stop before dispatch and ask whether to inherit for this run; never count a silent inherited fallback as meeting the floor. An unavailable economy selection falls back to inherit without blocking. After completion, prefer the model/effort the host reports; otherwise record `selected:<model>; underlying model unreported`.
