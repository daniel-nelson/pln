Then run the fixes. **Cluster the to-fix findings by file/subsystem** — findings touching the same files go in one cluster, so no two fix agents edit the same file. Spawn one fresh-context agent per cluster, **one at a time**, waiting for each to return before starting the next: the orchestrator commits between clusters, so the tree has to settle first. Each cluster's fix agent brief:
