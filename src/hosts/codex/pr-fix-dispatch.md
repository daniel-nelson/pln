Dispatch from `fix-manifest.tsv`, not an improvised file grouping. Before each isolated wave, the coordinator creates one standard detached git worktree per cluster beneath a bounded temporary run root and records every exact path. Spawn one fresh-context agent per cluster with its worktree and lease, launching all disjoint wave members before waiting. Original-tree clusters run alone. Every brief has `commit owner: coordinator`; workers touch neither git nor `REVIEW.md`. Nested CLI fallback workers remain serial.
