Dispatch from `fix-manifest.tsv`, not an improvised file grouping. Clusters run one at a time in the working tree the run was launched in: for each cluster `pln-scheduler ready` names, spawn one fresh agent with its brief and exact lease, and record its identity and `running` state before waiting. Do not create a git worktree. One worker owns the tree at a time. Every brief has `commit owner: coordinator`; workers touch neither git nor `REVIEW.md`. Nested CLI fallback workers remain serial.
