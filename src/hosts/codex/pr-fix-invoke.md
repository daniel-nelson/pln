Wait through the bounded mailbox loop until the cluster is complete, blocked, or failed; a child whose last observed status is `running` is never closed. A failed agent, empty result, malformed output, or write outside the lease fails that node; preserve its artifacts and force a fresh boundary. Otherwise the coordinator validates the result and diff, stages only explicit leased paths with `git add -- <paths>`, and commits the cluster with the required co-author trailer; record its result/route/commit as checkpointed, integrate it after dirty-state validation, and publish the complete next-generation `REVIEW.md` candidate through `pln-publish-review` before the next cluster starts. When the next cluster is a `reuse` node in the same cohort, continue that idle identity with its brief; otherwise spawn a fresh agent. A blocked cluster stops new dispatch: persist its handoff and agent identity through the same publisher, ask one question, then continue the same idle agent with the answer; if the identity is gone, spawn a fresh agent on the retained artifacts. Unexpected writes, conflicts, malformed output, or a corrected premise force a fresh boundary. Nested CLI fallback workers remain serial.
