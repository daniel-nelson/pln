Wait until every cluster in the wave completes, blocks, or fails. Validate result and lease before the coordinator stages explicit paths and commits each completed cluster in its own worktree. Record its result/route/commit as checkpointed, then integrate commits in manifest order and publish the complete next-generation `REVIEW.md` candidate through `pln-publish-review`. A blocked cluster freezes new dispatch; successful isolated siblings may checkpoint but do not integrate. Persist its handoff/worktree/agent identity through the same publisher, ask one question, then `send_input` to the same idle agent; if the identity is gone, spawn a fresh agent on the retained artifacts. Unexpected writes, conflicts, malformed output, or a corrected premise force a fresh boundary. Nested CLI fallback workers remain serial.
