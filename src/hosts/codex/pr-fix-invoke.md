Wait until every cluster in the wave completes, blocks, or fails. Validate result and lease before the coordinator stages explicit paths and commits each completed cluster in its own worktree. Record its result/route/commit as checkpointed, then integrate commits in manifest order and update `REVIEW.md` alone. A blocked cluster freezes new dispatch; successful isolated siblings may checkpoint but do not integrate. Persist its handoff/worktree/agent identity, ask one question, then use `followup_task` on the same idle agent; if the identity is gone, spawn a fresh agent on the retained artifacts. Unexpected writes, conflicts, malformed output, or a corrected premise force a fresh boundary. Nested CLI fallback workers remain serial.
