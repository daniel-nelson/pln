Dispatch exactly the validated same-model roster slots as fresh native subagents. Start independent slots concurrently and wait through the shared mailbox; this is review fan-out, not a dependency that requires serialization. Each assignment names its role, exact source fingerprint, schema, and distinct raw evidence path. A missing pointer or empty artifact is a failed slot.

Do not use `codex review` to replace the roster's broad judgment worker: its subprocess model and context are not controlled by the coordinator contract. It may be captured file-first as additive evidence. The native broad worker inherits the hosting session model and records unreported model identity truthfully when the host does not expose it.

When native multi-agent tools are unavailable, disclose the fallback and run one read-only `pln-codex-agent` call per same-model roster slot, serially because nested CLI processes share the login boundary. The peer adversarial slot, when permitted, substitutes for the local adversarial brief rather than adding a reader.
