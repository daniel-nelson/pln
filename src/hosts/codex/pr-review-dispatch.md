Otherwise run the army in two stages. First `codex review --base <base>`, which is purpose-built for exactly this and runs as a single process. Then the lenses below, **one at a time** — concurrent `codex` processes race on the shared OAuth token file, so parallel reviewers are not an option here. Each lens is one fresh-context agent (see Spawning a fresh-context agent) carrying the brief and the findings schema below. Six lenses plus one adversarial generalist:
