Run the lenses in sequence, one spawn each, and say so in one sentence up front ("Reviewing the diff with N fresh reviewers, one at a time") so the wall-clock cost isn't a surprise. If one reviewer fails, continue with the rest — partial coverage beats none. Track how many actually completed: a reviewer that returned an empty findings array counts as a success (it looked and found nothing); one that errored, timed out, or wrote an empty output file does not. Carry that count into the gate below.

**Cross-model coverage.** The Claude-side pass has no equivalent on this host, so there is no second model to consult unless the user has another agent CLI installed and authenticated. Don't invent one: note in a line that the review was single-model and move on. `codex review` plus the lenses above is the coverage here.
