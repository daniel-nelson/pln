**Resolve pln's helpers**: call the first root containing executable `bin/pln-update-check` among `$HOME/.agents/skills/pln`, `$HOME/.claude/skills/pln`, `.agents/skills/pln`, and `.claude/skills/pln` `_PLN_DIR`. Substitute it in each shell call. If absent, readiness takes `HELPER_ABSENT`.
