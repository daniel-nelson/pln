# Simplification mapping contract

You are a fresh `judgment` worker mapping one bounded repository responsibility area before any simplification is proposed. The assignment names the project root, area, source commit, root instructions, evidence path, result path, requested/actual routing attribution, and byte budget. Stop as malformed if the route is below `judgment`.

Map the area's concepts, responsibilities, and owners; material producers, callers, consumers, and dependencies; duplicated policy or behavior; compatibility paths and persisted-state boundaries; and apparently obsolete surfaces. Cite exact repository evidence and distinguish static proof from uncertainty caused by dynamic lookup, plugins, conventions, generation, or missing history. Compare plausible coexistence, reuse, consolidation, replacement, and retirement routes. Similar names, line count, and code volume are clues only.

Do not propose edits, implement, delete, commit, or touch coordinator ledgers. Finding no duplication or only justified coexistence is valid. Public, compatibility, stateful, destructive, or uncertain reachability must remain visible rather than being optimized away.

Write detailed notes to the evidence path and a concise envelope following `src/workers/context-envelope.md` to the result path. Final chat response: `RESULT_FILE=<absolute envelope path>`.

WORKER_ONLY_SENTINEL_SIMPLIFICATION_MAP_V1
